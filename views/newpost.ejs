<% include ./partials/header %>

<div class="ui main text container segment">
    <div class="ui huge header">Post Question</div>
    <form class="ui form" action="/dash" method="POST">
        <div class="field">
            <label>Title</label>
            <input type="text" name="new[title]" placeholder="Title">
        </div>
        <div class="field">
            <label>Description</label>
            <textarea name="new[description]"></textarea>
        </div>

        <button type="submit" class="ui violet big basic button">Submit</button>
    </form>
</div>

<% include ./partials/footer %>
